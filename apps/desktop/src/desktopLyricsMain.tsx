import React from 'react';
import ReactDOM from 'react-dom/client';
import { DesktopLyricsOverlayApp } from './DesktopLyricsOverlayApp';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <DesktopLyricsOverlayApp />
  </React.StrictMode>
);
